<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
        Dependencies, versions and the jar/shade build are all inherited.
        See https://github.com/DSquare68/homeforge-plugin-template
    -->
    <parent>
        <groupId>com.github.dsquare68</groupId>
        <artifactId>homeforge-plugin-starter-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath/>
    </parent>

    <artifactId>gym</artifactId>
    <packaging>jar</packaging>

    <name>Gym Tracker</name>
    <description>Track workouts, personal records and progress charts.</description>

    <properties>
        <!--
            ================================================================
            PLUGIN CONFIGURATION
            ================================================================
            plugin.path   : URL path served by HUB - https://hub.local/gym
            plugin.schema : PostgreSQL schema for this plugin's tables
            The parent's maven-jar-plugin config writes these into
            MANIFEST.MF; keep PluginInfo.java in sync with them.
            ================================================================
        -->
        <plugin.path>/gym</plugin.path>
        <plugin.schema>gym_schema</plugin.schema>
        <plugin.bootstrap.class>com.github.dsquare68.gym.PluginBootstrap</plugin.bootstrap.class>

        <!--
            Jackson 3 (tools.jackson) - gym-specific: it parses this plugin's
            exercise seed file. Same version flyway-core pulls in transitively,
            so the shaded jar ends up with one JSON stack rather than two.
        -->
        <jackson.version>3.1.5</jackson.version>
    </properties>

    <dependencies>
        <!--
            Reads db/seed/exercises_merged_final_1.0.json at install time.
            Declared explicitly because our code uses it directly - it must not
            silently disappear the day Flyway drops or upgrades it.
        -->
        <dependency>
            <groupId>tools.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
